Aineo Music v43.1.16

This build adds a denser split-wing spectrum with double the visible bar count, a darker neon blue-to-purple gradient on both axes, and faster open/close beat motion while keeping the stable working audio path intact.


v43.1.16 adds a light shape/balance polish to the split-wing spectrum without changing the underlying stable audio behavior.
